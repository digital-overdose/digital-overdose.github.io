import { Post } from './post';
/**
 * Represents a tag that a {@link Post} can be flagged by, usually to make it easier to find.
 *
 * @export
 */
export interface Tag {
  /** The name of the category / tag. */
  name: string;
}
