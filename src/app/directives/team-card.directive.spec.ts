import { TeamCardDirective } from './team-card.directive';

describe('TeamCardDirective', () => {
  it('should create an instance', () => {
    const directive = new TeamCardDirective();
    expect(directive).toBeTruthy();
  });
});
